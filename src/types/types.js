//redux
// primero se crea el type
// Segundo se crea el reducer
// tercero se crean los actions
export const types = {
    login: '[Auth] login',
    //hacemos otra funcion creando un type para el registro
    register: '[Auth] register'
}